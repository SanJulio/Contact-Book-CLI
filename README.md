Contact Book (Python CLI)

This is a simple command-line contact book written in Python.

The program allows the user to add contacts, view all saved contacts, search contacts by name, and delete contacts.  
All data is stored in memory while the program is running.
